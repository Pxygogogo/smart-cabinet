<view class="article" wx:for="{{model}}" wx:key="item" data-id="{{item.id}}" bindtap="goToDetail" >
  <view class="img-box">
    <image src="{{item.focus_img}}"></image>
  </view>
  <view class="title">
    {{item.title}}
  </view>
</view>